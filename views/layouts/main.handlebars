<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Tech Thoughts</title>
    <link rel='stylesheet' href='/css/jass.css' />
    <link rel='stylesheet' href='/css/style.css' />
  </head>

<div class="full-window">
  <body>
    <div class='flex-column justify-space-around'>
      <header
        class='HEADER display-flex justify-space-between align-center p-2'
      >
        <h1>
          <span role='img' aria-label='money'>💻</span>
          <a href='/' class='header-text'>Welcome to Tech Thoughts!</a>
        </h1>
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
            <a href='/profile' class='header-text'>profile</a>
            <span class="header-text">|</span>
            <button
              class='no-button'
              id='logout'
              class='header-text'
            >logout</button>
          {{else}}
            <a href='/login' class='header-text'>login</a>
          {{/if}}
        </nav>
      </header>
      <main class='container container-fluid mt-5'>
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src='/js/logout.js'></script>
    {{/if}}
  </body>

  <footer>
    <div class="footer">
      <p>Created by: <a href="https://github.com/joeytraffic" class="footer-link">Joey Traffic</a></p>
  </footer>
</div>
</html>